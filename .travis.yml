dist: xenial   # required for Python >= 3.7
language: python
python:
  - '3.6'

# To install packages which are not on pip
#before_install:
#  - sudo apt-get install ...
#addons:
#  apt:
#    update: true

# command to install dependencies
install:
  - pip install -r requirements.txt
  - python install_requirements.py  # install the dependencies of the WAM apps

# blocklist
#branches:
#  except:
#  - branch_name1
#  - branch_name2

# safelist
#branches:
#  only:
#  - branch_name1
#  - branch_name2
